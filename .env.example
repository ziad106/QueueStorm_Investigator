# All optional. The service runs fully without any of these (deterministic mode).
PORT=8000

# Enable the optional LLM text-polish layer (Google Gemini). Leave unset for pure rule-based.
# LLM_PROVIDER=gemini
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash
# LLM_TIMEOUT=8
